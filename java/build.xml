<project name="EarlyWarning" default="dist">
	<target name="init" description="Creates the needed directories.">
		<mkdir dir="bin"/>
		<mkdir dir="dist"/>
	</target>

	<target name="clean" description="Cleans up the build and dist directories.">
		<delete dir="bin"/>
		<delete dir="dist"/>
	</target>

	<target name="compile" description="Compiles the source files to the bin directory">
		<javac srcdir="src" destdir="bin"/>
	</target>
	
	<target name="dist" description="Packages up EarlyWarning into EarlyWarning.jar">
		<jar destfile="dist/EarlyWarning.jar" basedir="bin"/>
	</target>
</project>